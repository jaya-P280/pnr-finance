import SectionPage from "../../components/layout/SectionPage";

export default function Expenses() {
  return (
    <SectionPage
      title="Expenses"
      subtitle="Track expense entries and accounting reports here once the expense backend is ready."
    />
  );
}
